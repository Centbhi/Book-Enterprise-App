Button for cart

Checkout
Cart
Association of User & Books in Cart  
    Order Model
        Integer id
        User user
        Map<Book,Integer> book -> quantity
        double totalCost
        Boolean isOrdered
        Boolean isFulfilled
        LocalDateTime orderDate
        LocalDateTime dateFulfilled

Make orderDate null when sending update
Make genre an enum in backend instead of being in frontend service