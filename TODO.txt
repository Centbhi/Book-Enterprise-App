
Same issue that happened to Genre will happen in Order, must override CU (CU for calculating totalCost, U for aforementioned bug)
Checkout
Cart
Association of User & Books in Cart  
    Order Model
        Integer id
        User user
        Map<Book,Integer> book -> quantity
        double totalCost
        Boolean isOrdered
        Boolean isFulfilled
        LocalDateTime orderDate
        LocalDateTime dateFulfilled

Make orderDate null when sending update
Make genre an enum in backend instead of being in frontend service